#include "Common/World/WorldLoader.hpp"
#include "Common/World/WorldManager.hpp"

/*#include <jsoncons/json.hpp>
using jsoncons::json;
using jsoncons::pretty_print;*/

World* loadWorld(string filename)
{
	//json worldFile = json::parse_file(filename);
	//World* world = WorldManager::instance->createWorld(worldFile["ID"].as<WorldId>(), (WORLDTYPE)worldFile["type"].as<int>());

	return nullptr;//world;
}